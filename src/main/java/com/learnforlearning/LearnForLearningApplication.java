package com.learnforlearning;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for "Tanulás a tanulásért" — an ELTE informatics elective-subject
 * recommender.
 */
@SpringBootApplication
public class LearnForLearningApplication {

    public static void main(String[] args) {
        SpringApplication.run(LearnForLearningApplication.class, args);
    }
}
